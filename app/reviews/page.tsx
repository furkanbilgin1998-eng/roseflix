import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link";
import { Score } from "@/components/ui"; import { ArrowUpRight } from "lucide-react";
import { T } from "@/components/language";
import { getCmsContent } from "@/lib/cms";
export const metadata:Metadata={title:"Movie Reviews",description:"Independent, thoughtful reviews of new and essential movies."};
export default async function Reviews(){const {reviews}=await getCmsContent();return <div className="container-main py-16 md:py-24"><p className="eyebrow"><T en="The Verdict" tr="Karar"/></p><h1 className="display mt-3 max-w-3xl text-6xl font-semibold leading-none md:text-8xl"><T en="Reviews with a point of view." tr="Bakış açısı olan incelemeler."/></h1><p className="mt-5 max-w-xl text-sm leading-7 text-muted"><T en="No recycled consensus. No empty hype. Just honest criticism from writers who love the medium." tr="Tekrarlanmış fikirler ve boş övgüler yok. Sinemayı seven yazarlardan dürüst eleştiriler var."/></p><div className="mt-14 grid gap-5 md:grid-cols-2">{reviews.map(({movie,excerpt,author,date})=><Link key={movie.slug} href={`/movies/${movie.slug}`} className="group grid grid-cols-[115px_1fr] gap-5 border-t border-white/10 py-5 sm:grid-cols-[150px_1fr]"><div className="relative aspect-[2/3] overflow-hidden"><Image src={movie.poster} alt="" fill className="object-cover transition duration-500 group-hover:scale-105"/></div><div className="py-2"><Score value={movie.rating}/><h2 className="display mt-4 text-3xl font-semibold md:text-4xl">{movie.title}</h2><p className="mt-3 line-clamp-3 text-sm leading-6 text-muted"><T en={excerpt} tr={`${movie.title}, iddialı fikrini karakterlerin seçimleri üzerine özenle işlenmiş bir hikâyeye dönüştürüyor.`}/></p><p className="mt-5 text-[10px] font-bold uppercase tracking-widest text-blush">{author} · {date}</p><span className="mt-5 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest"><T en="Read review" tr="İncelemeyi oku"/> <ArrowUpRight size={14}/></span></div></Link>)}</div></div>}
