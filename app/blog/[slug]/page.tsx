import type { Metadata } from "next"; import Image from "next/image"; import { notFound } from "next/navigation"; import { articles, movies } from "@/data/content"; import { MovieCard, Newsletter } from "@/components/ui";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params; const a=articles.find(x=>x.slug===slug);return{title:a?.title||"Journal",description:a?.excerpt}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <><article><header className="container-main py-16 text-center md:py-24"><p className="eyebrow">{a.category} · {a.read} read</p><h1 className="display mx-auto mt-5 max-w-4xl text-6xl font-semibold leading-[.9] md:text-8xl">{a.title}</h1><p className="mx-auto mt-6 max-w-xl text-sm leading-7 text-muted">{a.excerpt}</p><p className="mt-6 text-[10px] font-extrabold uppercase tracking-widest">By Maya Ellis · June 8, 2026</p></header><div className="relative aspect-[16/8] max-h-[680px]"><Image src={a.image} alt="" fill priority className="object-cover"/></div><div className="container-main grid py-14 lg:grid-cols-[1fr_2fr_1fr]"><div className="hidden lg:block"><p className="eyebrow">Filed under</p><p className="mt-2 text-sm text-muted">{a.category}</p></div><div className="space-y-6 text-base leading-8 text-white/75"><p className="display text-3xl leading-10 text-white">Great films do more than hold our attention. They change the temperature of a room, then follow us into the night.</p><p>This year’s most compelling work shares a willingness to take its time. These are films made with conviction: personal enough to feel specific, ambitious enough to open onto something larger.</p><p>Our selections favor craft, emotional clarity, and the kind of images that remain vivid days later. Rankings are useful; conversation is better. Consider this a place to begin.</p></div></div></article><section className="container-main pb-12"><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{movies.slice(0,5).map(m=><MovieCard key={m.slug} movie={m}/>)}</div></section><Newsletter/></>}
