import type { Metadata } from "next"; import { ArticleCard } from "@/components/article-card"; import { articles } from "@/data/content"; import { Newsletter } from "@/components/ui";
export const metadata:Metadata={title:"Journal",description:"Features, rankings, interviews, and thoughtful stories about cinema."};
export default function Blog(){return <><div className="container-main py-16 md:py-24"><p className="eyebrow">The Roseflix Journal</p><h1 className="display mt-3 text-6xl font-semibold md:text-8xl">Cinema, considered.</h1><div className="rose-line mt-8"/><div className="mt-12"><ArticleCard article={articles[0]} wide/></div><div className="mt-14 grid gap-x-7 gap-y-12 md:grid-cols-2">{articles.slice(1).map(a=><ArticleCard article={a} key={a.slug}/>)}</div></div><Newsletter/></>}
